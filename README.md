# ChromeHound
Chrome credential harvester + keylogger. Decrypts Chrome saved passwords (AES-GCM), captures keystrokes in real-time, sends everything via Discord webhook. Runs with zero visible windows, auto-installs to startup, survives reboot. Single Python file or standalone EXE.
